import React from 'react'

function Props(props) {

    // Props : props stands for properties 
    // props are used to send data one component to another.

  return (
    <div>
      {props.age}
      {props.age}
      {props.age}
      {props.age}
    </div>
  )
}

export default Props
